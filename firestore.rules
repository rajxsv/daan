rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone (even unauthenticated) to read or write any document
    match /{document=**} {
      allow read, write, update: if request.time < timestamp.date(2030, 3, 25);
    }
  }
}
