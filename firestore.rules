rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone (even unauthenticated) to read or write any document
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
